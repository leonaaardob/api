import { MatchServerMiddlewareMiddleware } from "./match-server-middleware.middleware";

describe("MatchServerMiddlewareMiddleware", () => {
  it("should be defined", () => {
    expect(new MatchServerMiddlewareMiddleware()).toBeDefined();
  });
});
